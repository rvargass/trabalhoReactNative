/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ListaTarefa from './src/screens/ListaTarefa';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ListaTarefa);
